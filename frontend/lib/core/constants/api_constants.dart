// Use your machine's local IP address. Do not use localhost or 127.0.0.1.
// In Windows, you can find this by running `ipconfig` in the command prompt.
// In macOS/Linux, you can find this by running `ifconfig` in the terminal.
const String baseUrl = 'http://192.168.10.158:3000';
